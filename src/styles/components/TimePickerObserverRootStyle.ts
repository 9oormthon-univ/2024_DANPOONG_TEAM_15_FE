import styled from 'styled-components';
import {COLOR} from '@/const/color';

export const ObserverRoot = styled.div`
  width: 100%;
  height: 52px;
  border-top: 0.5px solid ${COLOR.ORANGE_01};
  border-bottom: 0.5px solid ${COLOR.ORANGE_01};
  display: flex;
  align-items: center;
`;
